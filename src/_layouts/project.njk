{% extends "./base.njk" %}

{% block content %}
<div class="stripe-content">
    <h2>{{ title }}</h2>
        <div class="project-content">
            {{ content | safe }}
        </div>
</div>
{% endblock %}