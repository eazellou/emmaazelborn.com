---
layout: base.njk
title: Emma Azelborn
permalink: /
---

<section>
    hello I'm emma I like music and stuff
</section>

<section>
    {% for project in collections.highlightedProjects %}
        <div>
            <h2>{{ project.data.title }}</h2>
            <!-- <p>{{ project.data.description }}</p> -->
        </div>
    {% endfor %}
    <a href="/projects">View all projects</a>
</section>

<!-- <section>
    recent posts
    <ul>
        {% for post in collections.posts %}
            <li>
                <a href="{{ post.url }}">
                    {{ post.data.title }}
                    <em>{{ post.data.date | date("LLLL d, yyyy") }}</em>
                </a>
            </li>
        {% endfor %}
    </ul>
</section> -->

<section>
    footer
</section>